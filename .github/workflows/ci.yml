name: CI

on:
  push:
    branches: [dev, main, hard-reset]
  pull_request:
    branches: [dev, main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: brief
          POSTGRES_PASSWORD: brief
          POSTGRES_DB: brief
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U brief -d brief"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      BRIEF_DATABASE_URL: postgresql://brief:brief@localhost:5432/brief

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci

      - run: npm run typecheck

      - run: npm test
