TaskNode schema (v1)
====================

The personal client is built around TaskNode — a single, strict schema for work items.

Table: task_nodes
-----------------

| Column        | Type        | Required | Notes                          |
|---------------|-------------|----------|--------------------------------|
| id            | TEXT (UUID) | yes      | Primary key                    |
| user_id       | TEXT        | yes      | Owner scope                    |
| label         | TEXT        | yes      | Human-readable title           |
| status        | TEXT        | yes      | open, in_progress, done, cancelled |
| due_at        | TIMESTAMPTZ | no       | Due datetime                   |
| scheduled_at  | TIMESTAMPTZ | no       | Planned start                  |
| completed_at  | TIMESTAMPTZ | no       | Set when status becomes done   |
| priority      | TEXT        | no       | low, normal, high, urgent      |
| description   | TEXT        | no       | Longer body text               |
| created_at    | TIMESTAMPTZ | yes      | Row created                    |
| updated_at    | TIMESTAMPTZ | yes      | Last mutation                  |

API shape (camelCase JSON)
--------------------------

{
  "schemaVersion": 1,
  "id": "uuid",
  "userId": "string",
  "label": "string",
  "status": "open",
  "dueAt": "ISO-8601",
  "scheduledAt": "ISO-8601",
  "completedAt": "ISO-8601",
  "priority": "normal",
  "description": "string",
  "createdAt": "ISO-8601",
  "updatedAt": "ISO-8601"
}

Source of truth
---------------

- Zod schema: client/personal/schema/task-node.ts
- SQL migration: db/migrations/001_task_nodes.sql
- API: GET/POST/PATCH /api/v1/tasks

Clients
-------

- personal  — implemented
- livestock — placeholder (client/livestock/)
- fishing   — placeholder (client/fishing/)
